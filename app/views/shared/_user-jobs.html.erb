<h3>Work experiences</h3>
<% @user.jobs.each do |job| %>
  <div class="timeline">
    <div class="entry">
      <div class="title">
        <h5> <%= job.start_date.year %> • <%= job.end_date.year %></h5>
        <ul>
          <li><%= job.title %> </li>
          <li><%= job.company.industry %> </li>
          <li><%= job.city %> </li>
          <% if job.logo%>
          <li class="mt-1"><%= image_tag job.logo, class:"avatar-logo"%> </li>
          <%end%>
        </ul>
      </div>
      <div class="body list-unstyled">
        <ul class="list-unstyled">
          <% if job.year_experience > 0 %>
            <li><%= job.year_experience %> years of work experience </li>
          <% else %>
            <li> 1 year of work experience </li>

          <% end %>
        </ul>
        <p><%= job.description %></p>
      </div>
    </div>
  </div>
<% end %>
